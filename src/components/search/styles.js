import styled from 'styled-components';

export const Wrapper = styled.div`
  grid-area: input;
  justify-self: center;
`;

export const Input = styled.input`
  font-size: 32px;
  margin-bottom: 24px;
`
