type Person {
  id: ID!
  index: Int
  picture: String
  age: Int
  eyeColor: String
  name: String
  company: String
  email: String
  phone: String
  friends: [Person]
  greeting: String
}

type Query {
  list(name: String): [Person]
}
